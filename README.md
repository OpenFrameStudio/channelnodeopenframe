# ChannelNode

A local YouTube channel workspace for collecting ideas, hooks, notes, research, publishing dates, and production tasks.

Open `index.html` in a browser to use it. The app stores your workspace in browser local storage and includes a JSON export button in the top bar.
